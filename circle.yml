machine:
  environment:
    CC: clang
    CXX: clang++
dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install libgc-dev
    - cmake .
  override:
    - make
test:
  override:
    - ./lisp < test/test.scm
